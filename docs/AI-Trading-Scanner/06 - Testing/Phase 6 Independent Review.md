# Phase 6 independent review

Status: **FOUNDATION, SCHEDULER AND CAUSAL ORCHESTRATION ACCEPTED.** Phase 6 remains IN PROGRESS because replay/execution, accounting lifecycle, durability and runners are not implemented.

Independent review examined candidate `d56de875001ac3c18168d4efdd1f35c3f1b82ead` on branch `feat/phase-6-causal-simulation` and returned **REVIEW STATUS: CHANGES REQUIRED**. The review found that replay envelopes did not resolve or causally validate typed payloads, result summaries trusted caller-provided linkage, equivalent Decimal scales produced different Phase 6 identities, execution references were not validated across Phase 4–6 contracts, same-phase tie ordering remained caller-controlled, and several tests claimed more than they proved. No approval or merge is recorded.

The first remediation candidate `61a0d434b78275236cb13f2a527abc44ed010687` also received **REVIEW STATUS: CHANGES REQUIRED**. It added a typed immutable replay artifact registry, Decimal V2 canonicalization, semantic event ordering and a Phase 4→6 execution chain, but its COMPLETE check compared fill IDs without deriving portfolio state. It also failed to bind reservation amounts/downside to sizing, allowed dangling execution-resolution references and allowed next-bar selection to cross sessions. The earlier statement that unreconciled fill accounting already failed closed was therefore too broad. Both rejected review outcomes remain part of the record; neither candidate was approved or merged.

The second remediation derives chronological single-currency V1 accounting from the flat starting portfolio and every uniquely linked fill/position change. It applies notional and execution costs once, conserves cash/equity, derives open quantity/cost basis/unrealized P&L and closed-trade realized P&L, and requires every supplied portfolio snapshot and realized trade to equal the derived state. This is an in-memory validation foundation, not a transactional posting service, durable ledger or complete portfolio lifecycle.

The execution chain now requires the Phase 5 reservation to equal the authoritative sizing decision's quantity, reservation amount, modeled downside, instrument, currency, risk/configuration/ownership identities, authority dimensions, creation time and proposal expiry. Recomputed downstream identities do not make tampered economics valid.

Execution-resolution payload V2 gives unfilled/no-data/rejected outcomes an outcome-specific reason. Every resolution resolves a same-run order; FILL_READY additionally resolves one exact same-run market event and one matching fill. Terminal outcomes are unique and contradictory fill/unfilled states fail closed. COMPLETE artifacts require one terminal resolution per order.

V1 next-bar selection derives the proposal session from its exact causal data-slice hash and the authoritative XNYS calendar. The eligible bar must stay in that session and before both expiry and session close. No next-session open can satisfy an intraday order; early closes and DST use calendar data rather than hard-coded clock times.

Phase 6 identities now use a V2 canonical Decimal representation that removes insignificant scale, canonicalizes signed zero, and normalizes exponent forms without float conversion. This change is confined to the uncompleted Phase 6 contracts and leaves Phase 1–5 identity functions intact. The run manifest now binds `SEMANTIC_PAYLOAD_V1`; equal-time, equal-phase order derives from payload kind and immutable payload identity, with no caller-provided key.

A mandatory offline `ValidatedExecutionChain` reuses the Phase 4 proposal/decision and Phase 5 risk/sizing/reservation/approval contracts and binds them to the simulation manifest, frozen cost and execution configurations, simulated order, canonical dataset, next eligible same-session bar-open fill and position change. It grants no external execution authority.

The second-remediation workspace passes 587 tests: 480 Phase 1–5 regressions and 107 focused Phase 6 cases. The 28 added cases include the original unchanged-portfolio and economically tiny-reservation probes; duplicate/missing/foreign fill applications; cost, net P&L, equity and realized-trade forgeries; terminal-resolution dangling/contradictory/multiple states; and regular, early-close, DST and next-session execution boundaries. Automated results do not replace independent competent human re-review.

Independent review of second-remediation candidate `bd352fa6783a45e766ec9bb73b998b087517c8cd` returned **REVIEW STATUS: CHANGES REQUIRED**. Reservation economics, execution-resolution linkage and session-bounded selection were accepted as resolved. COMPLETE numerical reconciliation was only partially resolved because a same-time portfolio snapshot could sort before the position change it already reflected. The review also reproduced order-sensitive artifact identities: reversing registry tuples preserved the canonical event trace but changed the artifact identity.

The third remediation keeps `SEMANTIC_PAYLOAD_V1` as the sole event-order policy and adds an explicit payload-kind dependency rank inside its semantic key. At one portfolio-update timestamp, position changes precede realized-trade records and portfolio snapshots. COMPLETE reconciliation now resolves fills, applications and checkpoints by their positions in that canonical event trace rather than timestamp comparison. Order-insensitive artifact registry tuples are canonicalized by canonical event position, with immutable identity only as a deterministic fallback for invalid or unreferenced input. Shuffled equivalent registry input therefore produces one artifact and result identity without changing Phase 1–5 identity behavior.

The third-remediation workspace passes 596 tests: 480 Phase 1–5 regressions and 116 focused Phase 6 cases, including nine new equal-time/canonicalization cases. Final targeted review of candidate `57dda0d39e3f75b58943f2b968dc1f1fa00371f6` classified equal-timestamp causal ordering and collection canonicalization RESOLVED, found no regression in the three previously resolved blockers, and returned **REVIEW STATUS: PASS** with the explicit result **PHASE 6 FOUNDATION ACCEPTED — SCHEDULER IMPLEMENTATION MAY BEGIN**.

Scheduler candidate `7adebed776e75ce1e1ed22fe070a8357cec105e4` added a content-identified immutable canonical schedule and concurrency-safe in-memory cursor. Independent scheduler review returned **CHANGES REQUIRED** with two HIGH findings. First, public raw-event construction could create an executable schedule without proving typed payload existence, uniqueness, causal timestamp or manifest binding. Second, public arbitrary-position checkpoint creation allowed callers to recompute structurally valid content and skip or replay events. Ordering, deterministic identity, ordinary cursor behavior, concurrency and Phase 1–5 regressions passed; the two trust-boundary defects blocked acceptance.

The bounded remediation reuses `ReplayArtifactBundle` as the sole authoritative executable construction boundary. Raw `ReplayEvent` collections and independently reconstructed `ReplaySchedule` objects cannot create `DeterministicReplayScheduler`. Every executable scheduler therefore inherits the accepted bundle's manifest, typed-registry, uniqueness, foreign-payload and causal-time validation. For checkpoints, all scheduler handles for one exact schedule share one process-local logical cursor. Only `scheduler.checkpoint()` marks the current state resumable; the next consumption invalidates that checkpoint, and arbitrary, recomputed or stale checkpoint content cannot advance or rewind the cursor through the public API. Checkpoint content identity proves consistency but is not authentication. Process loss also loses the in-memory trust state, so durable cross-process resume and anti-rollback remain explicitly deferred to persistence/crash-recovery work. The remediation workspace passed 643 tests: 480 Phase 1–5 regressions and 163 Phase 6 cases, including 47 focused scheduler cases. Independent final review of candidate `4adf20c30bfca24223ee980d2d13759adc1f54ba` returned **REVIEW STATUS: PASS WITH RECOMMENDATIONS** and **PHASE 6 SCHEDULER ACCEPTED — CAUSAL ORCHESTRATION MAY BEGIN**. The only recommendation concerned local Obsidian UI state and was not a scheduler defect.

The causal-orchestration candidate adds a one-event adapter over the accepted scheduler. A consumed market event releases exactly one resolved canonical bar. The Phase 2 as-of view is intersected with the consumed market-event prefix before accepted Phase 3 calculations or Phase 4 evaluation, so later and identical-timestamp unconsumed bars remain invisible. The result binds the exact evidence/configuration/ownership chain. NO_TRADE causes no risk mutation. Proposals pass through Phase 5 preliminary risk and atomic coordinator revalidation, producing typed rejection or reservation evidence. Twenty-five focused adversarial cases cover this boundary, including all four strategy profiles, information parity, same-time isolation, shared duplicate-handle visibility, foreign scopes, unseen lineage and deterministic fresh-process reconstruction.

Candidate `7cd1a75a7fed926483e0fdbd778d26be8b30d609` added an exclusive scheduler lease, a read-only inspection view, exception-atomic cursor/capital/result publication and context-bound artifact/schedule/configuration validation. Final targeted review classified the external cursor bypass and exception-atomic transition findings RESOLVED. It left two provenance findings open: recomputed wrapper identities could still conceal fabricated Phase 3/4 derived values, and caller-supplied Phase 5 reservation/post-capital evidence was not compared with an authoritative transaction record. The review therefore returned **CHANGES REQUIRED**; no orchestration approval or merge is recorded.

The final bounded provenance remediation rebuilds the exact market slice from the canonical dataset and consumed schedule prefix. Independent reconstruction recomputes the accepted Phase 3 indicator snapshot and Phase 4 strategy decision and requires exact equality with both the recorded result and the immutable evidence retained at publication. Normal `advance()` uses its original calculation evidence and executes the strategy only once. Proposal transitions now retain an immutable process-local record of the exact Phase 5 attempt and pre/post coordinator snapshots alongside the published result. Validation requires exact equality and reconciles quantity, sizing, reserved amount/downside, reservation ownership and identity, capital deltas, reservation-count delta, revisions and accepted/rejected behavior. The same orchestration rollback path removes result and authoritative evidence together on failure.

Sixteen added adversarial and positive-control cases alter and reidentify EMA, RSI, ATR, VWAP, strategy decisions, zero-reservation snapshots, the exact `963.768273168` reservation amount, reservation identity, capital revisions, foreign Phase 5 evidence, accepted/rejected status and rejection reasons. Legitimate NO_TRADE, rejected and reserved results reconstruct deterministically, and a direct probe proves one strategy evaluation per normal advance. The remediation workspace passes 720 tests: 480 Phase 1–5 regressions and 240 Phase 6 cases, including 47 scheduler and 77 orchestration cases. Ruff lint/format and strict mypy pass.

Final targeted independent review of candidate `b76891f7a1d787c2c07d26c0f4dd6a42df55aada` independently reproduced the original Phase 3/4 and Phase 5 forgery attacks, exercised failure boundaries before strategy evaluation, after reservation mutation, after result publication and after cursor commit, and reran the complete verification baseline. It classified both provenance findings RESOLVED, found no scheduler/transaction, causal-visibility or authority regression, and returned **REVIEW STATUS: PASS** with the explicit result **PHASE 6 CAUSAL ORCHESTRATION ACCEPTED — REPLAY/EXECUTION MAY BEGIN**. This accepts only the bounded in-memory orchestration increment; it does not complete Phase 6.

Milestone 6.1 candidate `bac6b0db268c6eb39b8f1fd9a80deb2d5fa2e525` received **REVIEW STATUS: CHANGES REQUIRED**. The independent adversarial review reproduced six defects: orders could be created after their validity window and backdated to strategy evaluation; creation could publish a receipt without a projection; reidentified foreign terminal attribution passed structural validation; cancellation retry failed after terminalization; exhausted schedules could publish future-dated expiry; and one run identity admitted materially different liquidity configurations. No approval of that candidate is recorded.

The bounded remediation issues creation commands only at the exact authoritative orchestration cursor and uses that engine time as submission time; replaces receipt/projection/source/cancellation registries through one exception-atomic state root; validates reconstructed terminal/projection evidence against resolver-owned authority; records incomplete end-of-data at the current causal clock; embeds complete liquidity configuration in `simulation-run-manifest-v2` while preserving V1 identities; and returns an existing cancellation command before terminal-state rejection. The six original probes are permanent regressions, augmented by every-publication-boundary failure injection, stale-cursor, concurrency, identity-forgery and manifest-identity cases. Independent competent human re-review of the exact remediation is still required before formal acceptance.

### Independent technical review — Claude, 2026-09-18

Claude (Anthropic AI agent) performed an independent technical review of the exact bounded remediation committed as `4beecc5728ff40cb4b12d69cc71c74f8f4ac09f5`, reading the full diff against rejected candidate `bac6b0db268c6eb39b8f1fd9a80deb2d5fa2e525`, the current source of `order_resolution.py` and `models.py`, and the added/changed tests directly, rather than relying on the commit message. It independently confirmed all six previously reproduced defects resolved: order creation is bound to the exact authoritative causal cursor and rejects a stale or expired view before issuing or accepting a command; receipt, projection and source-result publication share one exception-atomic state root, verified by failure injection at every staging and publication boundary; terminal-evidence validation requires exact equality with resolver-owned authoritative state plus field-by-field cross-checks against the order/manifest/result, rejecting reidentified or forged attribution; cancellation retries return the original idempotent command before any terminal-state check, and a first late cancellation against an already-terminal order is recorded as a no-effect audit entry rather than failing or corrupting state; every end-of-data/expiry terminal outcome is stamped at the current authoritative causal clock with a hard invariant against future-dated evidence; and the V2 run manifest binds the complete liquidity configuration into the `run_id` content hash, independently verified again at resolver construction.

Verification executed by Claude against the unmodified working tree: `uv run --locked pytest` (772 passed, 0 failed, 0 errors, 0 skipped, including the 28 new remediation-specific adversarial and concurrency cases), `uv run --locked ruff check .` (all checks passed) and `uv run --locked mypy` (strict, no issues across 84 source files). No production code, test file, or other repository file was modified to obtain these results.

Two low-severity, non-blocking observations were found and remain open and unresolved — they were not fixed and are not treated as resolved: (1) the cancellation-versus-candidate-bar-open comparisons in `_resolve_terminal` use a strict `<`, leaving an untested boundary where a cancellation dated exactly at a candidate bar's `start_at` would fill rather than cancel; (2) `SimulationLiquidityConfiguration`/`calculate_liquidity_model_id` has no dedicated identity round-trip/stability test comparable to other content-identified Phase 6 models. Both are recorded here as follow-up items for the maintainer to triage, not as blockers of this review's conclusion.

**Technical review result:** Claude's independent review found no unresolved blocking defect against the six original findings, and full automated verification passed. **This is not formal project acceptance.** Per `docs/PROJECT_RULES.md`, order state/idempotency and execution/fill assumptions are risk-critical and require independent competent **human** review and a recorded reviewer/approval before merge; an agent's review, including this one, is explicitly insufficient on its own and does not substitute for it. **Formal project acceptance is pending** competent human review and recorded approval. **Merge authorization is not granted**; no commit, push, PR, or merge of this candidate is authorized by this review.

### Independent human review — Dekkerszz, GitHub PR #5, 2026-09-18

Independent competent human review was recorded in GitHub PR #5, "Phase 6.1 — Independent human review (do not merge)," explicitly scoped to Phase 6.1 and explicitly excluding merge of the feature branch into `main`. Reviewer: `Dekkerszz`. Reviewed commits: `bac6b0db268c6eb39b8f1fd9a80deb2d5fa2e525` (the original rejected candidate) and `4beecc5728ff40cb4b12d69cc71c74f8f4ac09f5` (the remediation), alongside Claude's AI review documentation at `a66242eea85ad5b05ed41a3eaea0661319bc2b7c`. The PR description states the review covers the six originally identified defects and the two remaining LOW-severity observations, and explicitly notes that Claude's reported 772 passing tests do not replace competent human review.

The reviewer recorded that the code was checked and no blocking issues were found. The reviewer explicitly recorded that they **did not independently execute the test suite**; the 772 passing tests referenced in the review are Claude's previously reported results, not independently reproduced by the reviewer. The reviewer explicitly accepted both outstanding LOW-severity observations from Claude's technical review as documented, non-blocking follow-up items — not as resolved: (1) the cancellation boundary at a candidate bar's exact opening time requires an explicit behavior specification and a regression test; (2) the liquidity-model identity requires a dedicated stability test. The reviewer recorded that neither item blocks approval.

**Decision: Approved, within the stated Phase 6.1 review scope.** Per both the PR description and the reviewer's own comment, this approval does **not** authorize merging the feature branch into `main` and does **not** approve the remaining Phase 6 milestones (6.2 onward); PR #5 itself is titled and described "do not merge."

**Formal Phase 6.1 acceptance:** with this recorded independent competent human approval added to Claude's independent technical review and the existing automated verification (772 tests, ruff, strict mypy), the independent-competent-human-review requirement in `docs/PROJECT_RULES.md` for this risk-critical change is now satisfied for the Phase 6.1 order-resolution/terminal-resolver scope, and Milestone 6.1 is **ACCEPTED**. The two LOW-severity observations remain open, accepted follow-up items, not closed by this acceptance. Merging PR #5 or the feature branch into `main`, and starting Milestone 6.2, remain separate decisions requiring explicit owner authorization and are not granted by this record.

### Milestone 6.2 — source_record_id validation fix, Codex technical review, 2026-09-18

Independent AI technical review by Codex examined commit `1179c356653de4ffa668a0441330f19e4e6a8fa5` on `feat/phase-6.2-cost-fx-foundation`, the narrowly scoped fix that closes the `FxObservationReference.source_record_id` blank-value gap: a whitespace-only `source_record_id` (e.g. `"   "`) previously passed silently whenever a valid `source_checksum` was also present, since the field only participated in the combined "at least one locator" check rather than having its own blank-rejection. The fix adds a dedicated field validator requiring `source_record_id` to be nonblank whenever supplied, independent of `source_checksum`; the field remains fully optional (`None` is unaffected), the existing "at least one of `source_record_id`/`source_checksum`" requirement is unchanged, and no identity-computation code (`calculate_fx_observation_id`, `_normalized_decimal_content`) was touched. Scope is limited to `src/ai_trading_scanner/simulation/fx.py` and `tests/test_simulation_fx.py`; `models.py` is unmodified.

**Codex review result: no blocking issues found** in this narrow fix.

**Verification executed by Claude** against the unmodified working tree: `uv run --locked pytest` (855 passed, 0 failed, 0 errors, 0 skipped), `uv run --locked ruff check .` (all checks passed) and `uv run --locked mypy` (strict, no issues across 88 source files). No production code, test file, or other repository file was modified to obtain these results.

**Nonblocking test-quality recommendation (Codex, not yet actioned):** pin a known FX V1 identity fixture — a regression test asserting a specific, hard-coded `fx_observation_id` value for a fixed input set, so any future unintended change to the identity-computation path is caught even if no other test happens to exercise that exact input. Recorded here as an open, non-blocking follow-up; not implemented by this commit.

**User functional approval, 2026-09-18:** having inspected commit `1179c356653de4ffa668a0441330f19e4e6a8fa5` on GitHub, the repository owner (`Monaa92`) stated: "Ik ben akkoord met de werking" ("I agree with how it works"). This is recorded as the owner's **functional approval of the intended `source_record_id` behavior only** — that a blank/whitespace-only `source_record_id` is correctly rejected even when a valid `source_checksum` is present, and that a `None` value or a valid nonblank value are correctly accepted. It is **not** an independent technical code review, and it is **not** formal human acceptance under `docs/PROJECT_RULES.md`'s risk-critical review requirement, which is a distinct, more rigorous process (the kind `Dekkerszz` performed for Phase 5 and Milestone 6.1 via a scoped GitHub PR review). Independent human technical review of this fix, and of Milestone 6.2 as a whole, remains pending.

**Separate pending follow-up (unchanged, not part of this fix):** the inherited `models.py` float-acceptance gap in its own private `_normalized_decimal_content` (affecting `calculate_simulated_order_id`, `calculate_simulated_fill_id`, `calculate_execution_model_id`, `calculate_cost_model_id`, `calculate_liquidity_model_id`, `calculate_simulation_run_id`) remains open and untouched — it requires its own authorization, its own commit, and its own review given it touches already-accepted Phase 1–6.1 code, per the design discussion recorded for this milestone.

**Scope of this record:** this section records Codex's AI technical review, Claude's automated verification, and the owner's functional approval of one narrow fix — none of these is independent competent human review, and per `docs/PROJECT_RULES.md` none by itself constitutes formal project acceptance of this fix. It is not Milestone 6.2 acceptance and does not authorize merging this branch, starting the FX resolver, or any other Phase 6.2 component. Independent human technical review of this fix remains pending; Milestone 6.2 as a whole remains unreviewed and unaccepted.

### Milestone 6.2 — FX observation-selection slice, focused Codex review, 2026-09-18

Codex performed a **focused** independent review of the pure causal FX observation-selection slice (`select_eligible_fx_observation`, `FxObservationUnavailable`, `FxObservationUnavailableReason` in `simulation/fx.py`) before it was committed, reviewing the actual uncommitted implementation directly rather than a description of it. The commit produced afterward is `d7e3667ed9ae8f84c36588eb29858a992f7637ae`.

**Reviewed correctness areas (four, specifically):** (1) causality — the selection never chooses an observation whose `available_at` is after the evaluation time, and only operates on an already causally released prefix; (2) `VALIDATED`-only eligibility — `UNVALIDATED` and `SUSPECT` observations are always excluded, with no override or toggle, per the owner's prior decision; (3) `observed_at`-based staleness — age is measured from `observed_at`, not `available_at`, so an observation already old when it became available cannot pass by being used immediately; (4) deterministic selection and fail-closed behavior — freshest-`observed_at`-wins with a documented tie-break, and typed `FxObservationUnavailable` evidence (never a fabricated or borrowed rate) when nothing qualifies.

**Codex review result: no critical findings** in these four reviewed areas.

**Verification independently executed by Codex:** the 16 focused selection tests in `tests/test_simulation_fx.py` — **16 passed, 64 deselected** (the remaining FX-contract tests in that file were not run in this pass).

**Codex did not run** in this pass: the full locked test suite, `ruff check`, `mypy`, or a comprehensive repository review. This was a focused review of the selection slice only.

**Verification executed by Claude** (reported previously, attributed here to Claude, not Codex): the full suite — 871 passed, 0 failed, 0 errors, 0 skipped; `ruff check .` — all checks passed; `ruff format --check .` — clean; `mypy` (strict) — no issues across 88 source files; `git diff --check` — clean.

**Scope of this record:** a focused review of one slice's four specific correctness areas, backed by a focused 16-test run from Codex and a separate full-suite/static-check run from Claude, is **not** a complete independent review of this slice, **not** independent competent human review, and per `docs/PROJECT_RULES.md` does **not** constitute formal acceptance of this slice, this fix, or any part of it. It does not authorize merging this branch, implementing currency conversion, quote-side selection, fees, or any further Phase 6.2 component. Independent human technical review of this slice, and Milestone 6.2 as a whole, remain pending and unaccepted.

Still absent: actual fill creation, reservation consumption/release, cost/FX calculation, transaction-safe fill/accounting, fixed protection, durable persistence, cross-process recovery, complete position/P&L lifecycle, final isolated runner, Agent 5, broker/provider/network/AI integrations, PAPER and LIVE. The target design is recorded in [[01 - Architecture/Execution/Replay and Simulation Architecture]], [[02 - Agents & Strategies/Autonomous Survival Agent]] and [[03 - Experiments/4+1 Historical Experiment]].

See [[06 - Testing/Phase 6 Completion Criteria]], [[06 - Testing/Backtesting]], [[00 - Project/Decisions/ADR-033 - Deterministic Phase 6 replay foundation]] and [[00 - Project/Current Status]].
